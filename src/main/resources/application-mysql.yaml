datasources:
  default:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${datasources.default.host:localhost}:${datasources.default.port:3306}/${datasources.default.database}
    dialect: MYSQL
